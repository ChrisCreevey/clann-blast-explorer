// charts.js — histograms and scatter plots, hand-written SVG (no charting library).
// TODO: Phase 2. %identity, e-value (log scale), bit score, alignment length,
// query coverage distributions; %identity vs coverage scatter.

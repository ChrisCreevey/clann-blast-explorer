// taxonomy.js — taxonomy bar/pie chart, hand-written SVG (no charting library).
// TODO: Phase 2. Uses staxids/sscinames when present; falls back to a
// genus/species heuristic parsed from stitle when absent (label as approximate).

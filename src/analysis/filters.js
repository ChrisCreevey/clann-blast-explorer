// filters.js — threshold/taxon filtering logic.
// TODO: Phase 2. Live filtering by %identity, e-value, bit score, alignment
// length, query coverage; exclude self-hits; top-N-per-query cap; taxon
// include/exclude when taxonomy present.
